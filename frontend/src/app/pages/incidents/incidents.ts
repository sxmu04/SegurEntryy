import {Component,OnInit} from '@angular/core';import{CommonModule}from'@angular/common';import{Router}from'@angular/router';import{DashboardService}from'../../core/services/dashboard.service';
@Component({selector:'app-incidents',standalone:true,imports:[CommonModule],template:`<main class="shell"><header><div><span>SegurEntry · Seguridad</span><h1>Alertas e incidentes</h1><p>Accesos denegados detectados en el historial del sistema.</p></div><button (click)="router.navigate(['/operaciones'])">Centro operativo</button></header><section class="panel"><div class="state" *ngIf="loading">Analizando accesos...</div><div class="state error" *ngIf="error">{{error}}</div><article *ngFor="let x of incidents"><div class="icon">!</div><div class="grow"><strong>{{x.user||x.name||'Acceso no identificado'}}</strong><p>{{x.email||x.document||'Sin identificación'}} · {{x.device||'Dispositivo no reportado'}}</p></div><time>{{format(x.date||x.created_at||x.createdAt)}}</time><button class="detail" (click)="detail(x)">Detalle</button></article><div class="state" *ngIf="!loading&&!error&&incidents.length===0">No se encontraron accesos denegados.</div></section></main>`,styles:[`:host{display:block;min-height:100vh;background:#f5f7f5;font-family:Inter,system-ui;color:#17211b}.shell{max-width:1250px;margin:auto;padding:clamp(18px,3vw,38px)}header{display:flex;justify-content:space-between;gap:20px;margin-bottom:22px}header span{color:#08783a;font-weight:800;text-transform:uppercase;font-size:.78rem}h1{font-size:clamp(2rem,4vw,3rem);margin:.35rem 0}p{color:#69766e}button{border:0;border-radius:12px;padding:11px 15px;background:#08783a;color:white;font-weight:800;cursor:pointer}.panel{background:#fff;border:1px solid #dce5df;border-radius:18px;padding:18px}article{display:flex;align-items:center;gap:14px;padding:16px;border-bottom:1px solid #edf1ee}.icon{width:38px;height:38px;border-radius:50%;display:grid;place-items:center;background:#fde8e8;color:#a82525;font-weight:900}.grow{flex:1}.grow p{margin:.25rem 0}time{color:#6c786f}.detail{background:#eef5f0;color:#08783a}.state{text-align:center;padding:30px;color:#68756d}.error{color:#a82525}@media(max-width:720px){header,article{flex-direction:column;align-items:flex-start}time{font-size:.85rem}}`]})export class Incidents implements OnInit{incidents:any[]=[];loading=true;error='';constructor(public router:Router,private dashboard:DashboardService){}ngOnInit(){this.dashboard.getAccesses().subscribe({next:(r:any)=>{const a=Array.isArray(r)?r:(r?.accesses||r?.results||[]);this.incidents=a.filter((x:any)=>!this.allowed(x));this.loading=false},error:()=>{this.error='No fue posible consultar el historial de accesos.';this.loading=false}})}allowed(x:any){return x?.allowed===true||String(x?.status||x?.result||'').toLowerCase().includes('permit')}format(v:any){if(!v)return'Sin fecha';const d=new Date(v);return isNaN(d.getTime())?String(v):d.toLocaleString('es-CO')}detail(x:any){const id=x.id||x.access_id||x.uid;if(id)this.router.navigate(['/accesos',id])}}